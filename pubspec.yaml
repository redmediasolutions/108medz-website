name: medzsite
description: A new Jaspr project
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.22.3
  jaspr_cli: ^0.22.1
  http: ^1.1.0
  jaspr_router: ^0.8.1
  firebase_dart: ^1.6.0
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.9
  jaspr_builder: ^0.22.1
  lints: ^5.0.0

jaspr:
  mode: server
  flutter: plugins
